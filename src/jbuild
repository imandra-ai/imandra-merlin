

(jbuild_version 1)

(executables
 ((names (imandra_merlin))
  (public_names (ocamlmerlin-imandra))
  (package imandra-merlin)
  (modes (byte))
  (libraries (compiler-libs.common merlin_extend ocaml-migrate-parsetree imandra-base.syntax imandra-reason-parser))
  ))

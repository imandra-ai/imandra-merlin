

(jbuild_version 1)

(executables
 ((names (imandra_merlin))
  (public_names (ocamlmerlin_imandra))
  (package imandra-merlin)
  (modes (byte))
  (libraries (compiler-libs.common merlin_extend ocaml-migrate-parsetree imandra.lib))
  ))
